---
import { roleTone } from "~/lib/tech";

interface Props {
  role: string;
}
const { role } = Astro.props;
---

<span
  class:list={[
    "inline-flex items-center rounded-full px-3 py-1 text-xs font-medium",
    roleTone(role),
  ]}
>
  {role}
</span>
